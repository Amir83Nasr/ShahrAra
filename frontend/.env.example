# ─── BACKEND REWRITE TARGET ──────────────────────────────────
# Used in next.config.ts rewrites to proxy /api/* requests.
# Default: http://localhost:8000
# Docker Compose: http://backend:8000
BACKEND_URL=http://localhost:8000